# Self-hosting

Kavrix does not require or ship a self-hosted server. The supported CLI connects
directly to MongoDB and performs encryption and decryption locally. This page is
kept only as a migration note for older repository layouts; the historical API,
sync, and server commands are not part of the npm package.
