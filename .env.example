# Required for opt-in API/storage integration tests.
# Copy the variable name into your shell or protected secret manager.
# Never commit a real username, password, token, or connection URI.
KAVRIX_MONGODB_URI=
